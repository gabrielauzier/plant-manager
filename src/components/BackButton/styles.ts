import styled from "styled-components/native";
import { BorderlessButton } from "react-native-gesture-handler";

export const Container = styled(BorderlessButton)`
  position: absolute;
  top: 48px;
  left: 24px;
` as unknown as typeof BorderlessButton;
